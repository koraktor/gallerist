<h1><%= @person.name %></h1>

<div class="row">
  <% @person.photos.order(:date).each do |photo| %>
    <%= partial :thumbnail, locals: { photo: photo } %>
  <% end %>
</div>

<%= partial :photo_modal %>
